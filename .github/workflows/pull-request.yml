name: Pull Request

on:
  pull_request:

jobs:
  build:
    uses: vfourny/.github/.github/workflows/build.yml@main
    with:
      node_version: '24'
      is_tested: false
      is_build_upload: false
